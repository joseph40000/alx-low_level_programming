#include <stdio.h>
#include <string.h>

/**
 *main - Main is where my project is being compiled
 *
 *
 * Return: return 0 everytime
 */
int main (void)
{
	char displayed[52];
	strcpy(displayed, "\"""programming is like building a multilinguall puzzle");
	puts(displayed);
	return (0);
}
